# Copy to .streamlit/secrets.toml (never commit real passwords)

# Supabase PostgreSQL — Project Settings → Database → Connection string (URI)
SUPABASE_DB_HOST = "db.YOUR_PROJECT_REF.supabase.co"
SUPABASE_DB_PASSWORD = "your-database-password"
SUPABASE_DB_USER = "postgres"
SUPABASE_DB_NAME = "postgres"
SUPABASE_DB_PORT = "5432"

# Or single URL:
# SUPABASE_DB_URL = "postgresql://postgres:PASSWORD@db.YOUR_PROJECT_REF.supabase.co:5432/postgres?sslmode=require"

ADMIN_PASSWORD = "your-admin-password"
DEEPSEEK_API_KEY = ""
